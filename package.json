{
  "name": "burson-ui-dev-portfolio",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "server/*"
  ],
  "scripts": {
    "bootstrap": "npm run -w @burson/ui build && npm run -w web dev",
    "test": "npm -ws run test",
    "build": "npm -ws run build",
    "lint": "npm -ws run lint",
    "format": "npm -ws run format",
    "ci": "npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^20.11.30",
    "eslint": "^9.6.0",
    "prettier": "^3.3.2"
  }
}